# shared util for cpa-sim
# eg: debug policy (toggle stages' debugging modes) -> pipeline
# (maybe there should be a separate policy file?)


def placeholder_utils() -> None:
    """Example utility—replace or remove."""
    pass
